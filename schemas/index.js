const addSchema = require("./addSchema");
const statusContactSchema = require("./statusContactSchema");
const registerSchema = require("./registerSchema");

module.exports = {
  addSchema,
  statusContactSchema,
  registerSchema,
};
